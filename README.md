# Yale Omeka Utilities

This is a half-hearted README to have at least a start.

`curators-list-with-emails.py` requires the presence of a file with API key information and more. It should look like this:
```
baseURL = 'https://onlineexhibits.library.yale.edu/'
identity = '[get from your Omeka S API key page]'
credential = '[get from your Omeka S API key page]'
```